name: Generate Documentation from Commit Diff

on:
  push:
    branches:
      - develop

jobs:
  generate-docs:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.x

      - name: Install poetry
        run: |
          python -m pip install --upgrade pip
          pip install poetry

      - name: Clone generate_readme repository
        run: |
          git clone https://github.com/YumaMatsumura/generate_readme.git
          cd generate_readme
          poetry install

      - name: Generate and commit documentation
        env:
          LANGUAGE: Japanese
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${GITHUB_REPOSITORY}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_MODEL: gpt-4o-2024-08-06
          TEMPLATE_PATH: ./template.json
          OUTPUT_PATH: ./README.md
          BRANCH_NAME: doc-update
        run: |
          cd generate_readme
          poetry run python generate_readme/generate_readme.py

